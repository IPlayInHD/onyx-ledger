node_modules/
dist/
dist-ssr/
coverage/
playwright-report/
test-results/
*.local
.vite/

# TypeScript incremental build state — a local artifact, not source.
*.tsbuildinfo

# Design-review captures: regenerate with ONYX_VISUAL=1; never a repo artifact.
visual/
