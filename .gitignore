node_modules/
.netlify/
*.log
# Build artifacts / generated deliverables
*.zip
server/data/db.json
server/dashboard-preview.png
.venv/
__pycache__/
*.pyc
.pytest_cache/
.env

# Terraform working directories and provider lock files. The lock file is
# deliberately NOT committed: nothing here is applied from a developer machine,
# and the pipeline resolves providers against the version constraints in each
# root every time.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
